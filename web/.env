# Privy app ID is a PUBLIC identifier (safe in client code). The app SECRET is NOT here —
# it lives only in the macOS Keychain (cairn / privy-app-secret) for server-side use.
VITE_PRIVY_APP_ID=cmqju6rfd00na0ck17ksme70p
