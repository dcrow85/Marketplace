# Supervised execution Phase 1 — rejected research artifact

`cairn-supervised-execution-v0.1` is not an active Cairn release candidate.
Its machine status is `rejected_research_only` in
[`rejection.json`](rejection.json): it is not advertised, deployable, or
reference-service-eligible.

Every frozen form, including exact commit
`c5646a0104664f7edf2a453450a2ec5d5d2eb82c`, was rejected by independent
review. Passing local and clean controls did not close material gaps around
authenticated service time, complete action/state/lineage reads,
future-effective activity heads, generic-object semantics, cursor/page
authentication, and structural provenance.

This directory is retained as falsification evidence and execution-vocabulary
research. Do not:

- deploy it;
- claim conformance;
- build an execution reference service from it;
- treat its 29 operations as an advertised Cairn surface; or
- cite its passing test totals as acceptance.

The active narrowed boundary is the proposal-only minimum trust kernel in
[`../minimum-trust-kernel.json`](../minimum-trust-kernel.json) and
[`../../Protocol_Agent_Minimum_Trust_Kernel_v0.1.md`](../../Protocol_Agent_Minimum_Trust_Kernel_v0.1.md).

Reopening an execution/read profile requires a separately specified and audited
signed service observation primitive first.
